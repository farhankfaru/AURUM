<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AURUM - Admin Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            height: 100%;
            overflow-y: auto;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #f8f6f3 0%, #ebe6e0 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            position: relative;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="none"><defs><pattern id="grain" patternUnits="userSpaceOnUse" width="100" height="100"><circle cx="25" cy="25" r="0.3" fill="%23D4C4A8" opacity="0.1"/><circle cx="75" cy="75" r="0.3" fill="%23D4C4A8" opacity="0.1"/><circle cx="50" cy="10" r="0.3" fill="%23D4C4A8" opacity="0.1"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>') repeat;
            pointer-events: none;
            z-index: -1;
        }

        .login-container {
            background: rgba(255, 255, 255, 0.95);
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(139, 115, 85, 0.15);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(139, 115, 85, 0.1);
            width: 100%;
            max-width: 420px;
            position: relative;
            z-index: 1;
            margin: 2rem auto;
        }

        .brand-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .brand-title {
            font-family: 'Playfair Display', serif;
            font-size: 2.2rem;
            color: #8B7355;
            font-weight: 400;
            letter-spacing: 0.15em;
            margin-bottom: 0.5rem;
        }

        .brand-subtitle {
            color: #6B5B47;
            font-size: 0.85rem;
            font-style: italic;
            font-weight: 300;
        }

        .admin-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: linear-gradient(135deg, #8B7355, #A0956B);
            color: white;
            padding: 0.4rem 1rem;
            border-radius: 25px;
            font-size: 0.75rem;
            font-weight: 500;
            letter-spacing: 0.5px;
            margin: 0.8rem 0 1.5rem 0;
            box-shadow: 0 4px 15px rgba(139, 115, 85, 0.3);
        }

        .form-title {
            text-align: center;
            font-size: 1.3rem;
            color: #2c2c2c;
            margin-bottom: 1.5rem;
            font-weight: 500;
        }

        .form-group {
            margin-bottom: 1.2rem;
            position: relative;
        }

        .form-label {
            display: block;
            color: #6B5B47;
            font-size: 0.85rem;
            font-weight: 500;
            margin-bottom: 0.4rem;
            letter-spacing: 0.5px;
        }

        .form-input {
            width: 100%;
            padding: 0.9rem 1rem;
            padding-left: 2.8rem;
            border: 2px solid rgba(139, 115, 85, 0.2);
            border-radius: 10px;
            font-size: 0.95rem;
            font-family: inherit;
            background: rgba(255, 255, 255, 0.8);
            color: #2c2c2c;
            transition: all 0.3s ease;
            outline: none;
        }

        .form-input:focus {
            border-color: #8B7355;
            background: white;
            box-shadow: 0 0 0 3px rgba(139, 115, 85, 0.1);
        }

        .input-icon {
            position: absolute;
            left: 0.9rem;
            top: 2rem;
            color: #8B7355;
            font-size: 1rem;
            pointer-events: none;
            transition: color 0.3s ease;
        }

        .password-toggle {
            position: absolute;
            right: 0.9rem;
            top: 2rem;
            color: #8B7355;
            cursor: pointer;
            font-size: 1rem;
            transition: color 0.3s ease;
        }

        .password-toggle:hover {
            color: #6B5B47;
        }

        .login-button {
            width: 100%;
            background: linear-gradient(135deg, #8B7355 0%, #A0956B 100%);
            color: white;
            padding: 0.9rem 2rem;
            border: none;
            border-radius: 10px;
            font-size: 0.95rem;
            font-weight: 600;
            letter-spacing: 0.5px;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 1.2rem;
            position: relative;
            overflow: hidden;
        }

        .login-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s ease;
        }

        .login-button:hover::before {
            left: 100%;
        }

        .login-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(139, 115, 85, 0.4);
        }

        .divider {
            text-align: center;
            margin: 1.5rem 0;
            position: relative;
            color: #8B7355;
            font-size: 0.8rem;
        }

        .divider::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 1px;
            background: rgba(139, 115, 85, 0.2);
            z-index: 1;
        }

        .divider span {
            background: rgba(255, 255, 255, 0.95);
            padding: 0 1rem;
            position: relative;
            z-index: 2;
        }

        .back-to-site {
            text-align: center;
            margin-top: 1.5rem;
        }

        .back-link {
            color: #8B7355;
            text-decoration: none;
            font-size: 0.85rem;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
            padding: 0.4rem 0.8rem;
            border-radius: 6px;
        }

        .back-link:hover {
            background: rgba(139, 115, 85, 0.1);
            transform: translateX(-3px);
        }

        .error-message {
            background: rgba(231, 76, 60, 0.1);
            border: 1px solid rgba(231, 76, 60, 0.3);
            color: #c0392b;
            padding: 0.8rem;
            border-radius: 6px;
            margin-bottom: 1rem;
            font-size: 0.85rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        @media (max-width: 768px) {
            body {
                align-items: flex-start;
                padding-top: 2rem;
                padding-bottom: 2rem;
            }

            .login-container {
                padding: 2rem;
                margin: 1rem 0;
                border-radius: 15px;
            }

            .brand-title {
                font-size: 2rem;
            }

            .form-input {
                padding: 0.8rem;
                padding-left: 2.5rem;
                font-size: 0.9rem;
            }

            .input-icon {
                left: 0.8rem;
                top: 1.95rem;
            }

            .password-toggle {
                right: 0.8rem;
                top: 1.95rem;
            }
        }

        @media (max-width: 480px) {
            body {
                padding: 1rem 0.5rem;
            }

            .login-container {
                padding: 1.5rem;
                max-width: 100%;
                margin: 0.5rem 0;
            }

            .brand-title {
                font-size: 1.8rem;
                letter-spacing: 0.1em;
            }

            .form-input {
                padding: 0.7rem;
                padding-left: 2.3rem;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .login-container {
            animation: fadeInUp 0.5s ease forwards;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="brand-header">
            <h1 class="brand-title">AURUM</h1>
            <p class="brand-subtitle">Timeless elegance, exclusive fashion</p>
            <div class="admin-badge">
                <i class="fas fa-shield-alt"></i>
                <span>Admin Portal</span>
            </div>
        </div>

        <h2 class="form-title">Admin Login</h2>

        <% if (typeof message !== 'undefined' && message) { %>
            <div class="error-message">
                <i class="fas fa-exclamation-circle"></i>
                <%= message %>
            </div>
        <% } %>

        <form action="/admin/login" method="POST">
            <div class="form-group">
                <label for="email" class="form-label">Email Address</label>
                <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    class="form-input" 
                    placeholder="Enter your admin email"
                    required
                    value="aurumadmin@gmail.com"
                >
                <i class="fas fa-envelope input-icon"></i>
            </div>

            <div class="form-group">
                <label for="password" class="form-label">Password</label>
                <input 
                    type="password" 
                    id="password" 
                    name="password" 
                    class="form-input" 
                    placeholder="Enter your password"
                    required
                >
                <i class="fas fa-lock input-icon"></i>
                <i class="fas fa-eye password-toggle" id="togglePassword"></i>
            </div>

            <button type="submit" class="login-button">
                <span>Login to Admin Panel</span>
            </button>
        </form>

        <div class="divider">
            <span>or</span>
        </div>

        <div class="back-to-site">
            <a href="/" class="back-link">
                <i class="fas fa-arrow-left"></i>
                <span>Back to Main Site</span>
            </a>
        </div>
    </div>

    <script>
        document.getElementById('togglePassword').addEventListener('click', function() {
            const passwordInput = document.getElementById('password');
            const toggleIcon = this;
            
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                toggleIcon.classList.remove('fa-eye');
                toggleIcon.classList.add('fa-eye-slash');
            } else {
                passwordInput.type = 'password';
                toggleIcon.classList.remove('fa-eye-slash');
                toggleIcon.classList.add('fa-eye');
            }
        });

        const errorMessage = document.querySelector('.error-message');
        if (errorMessage) {
            setTimeout(() => {
                errorMessage.style.opacity = '0';
                errorMessage.style.transform = 'translateY(-10px)';
                setTimeout(() => errorMessage.remove(), 300);
            }, 5000);
        }
    </script>
</body>
</html>
