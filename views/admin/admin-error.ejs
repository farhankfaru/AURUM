<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Error - AURUM</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600&family=Inter:wght@400;500&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(135deg, #f8f6f3 0%, #ebe6e0 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            color: #6B5B47;
            padding: 1rem;
        }

        .error-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(139, 115, 85, 0.15);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(139, 115, 85, 0.1);
            padding: 3rem 4rem;
            text-align: center;
            max-width: 550px;
            width: 100%;
            animation: fadeIn 0.5s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .error-code {
            font-family: 'Playfair Display', serif;
            font-size: 5rem;
            font-weight: 600;
            color: #8B7355;
            margin-bottom: 0.5rem;
            letter-spacing: 0.1em;
        }

        .error-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.75rem;
            font-weight: 500;
            color: #2c2c2c;
            margin-bottom: 1rem;
        }

        .error-message {
            font-size: 1rem;
            line-height: 1.6;
            margin-bottom: 2.5rem;
        }

        .back-button {
            display: inline-block;
            background: linear-gradient(135deg, #8B7355, #A0956B);
            color: white;
            padding: 0.8rem 2.5rem;
            border-radius: 30px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 8px 20px rgba(139, 115, 85, 0.3);
        }

        .back-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 30px rgba(139, 115, 85, 0.4);
        }

        @media (max-width: 600px) {
            .error-container {
                padding: 2rem;
            }

            .error-code {
                font-size: 4rem;
            }

            .error-title {
                font-size: 1.5rem;
            }

            .error-message {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-code">500</div>
        <h1 class="error-title">Oops! Something Went Wrong</h1>
        <p class="error-message">
            We've encountered an internal server error and our team has been notified.
            Please try again later or return to the main dashboard.
        </p>
        <a href="/admin/dashboard" class="back-button">Back to Dashboard</a>
    </div>
</body>
</html>

